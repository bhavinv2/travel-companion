Hi {{ (trip.poster_name or trip.display_name or '').split(' ')[0] or 'there' }},

A traveller on the same route as your {{ trip.route_display }} trip has just posted on Connecting Desis:

  {{ other.display_name }} — {{ other.route_display }} on {{ other.from_date or 'a similar date' }} ({{ best.score }}% match)
{% if count > 1 %}
  …and {{ count - 1 }} more possible companion{{ 's' if count > 2 else '' }}.
{% endif %}
Nobody's contact details have been shared yet. If you'd like to be introduced, open your dashboard and choose
"Share my contact & notify" on the match:

  {{ site_url }}/dashboard

Safe travels,
The Connecting Desis team
{{ site_url }} · {{ support_email }}
